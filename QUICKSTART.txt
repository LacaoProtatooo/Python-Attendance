# 🚀 QUICK START - RUN THIS APPLICATION

## Windows Users - 3 Ways to Run

### Method 1: Double-Click (EASIEST) ⭐
1. Open File Explorer
2. Navigate to: `C:\Users\joshz\Documents\GitHub\Python-Attendance`
3. **Double-click** `RUN.bat`
4. Application starts automatically

### Method 2: PowerShell
1. Open PowerShell in the project folder
2. Run: `.\RUN.ps1`
3. Application starts

### Method 3: Manual Command Line
```powershell
cd "C:\Users\joshz\Documents\GitHub\Python-Attendance"
python main.py
```

---

## First Install - Install Dependencies

If this is your first time, run once:

```powershell
pip install opencv-python opencv-contrib-python numpy pillow
```

**OR** let `RUN.bat` do it automatically!

---

## What Happens When You Run

```
1. Application window opens
2. You see the Main Interface with:
   - Camera feed area (left)
   - Control buttons (right)
3. Ready to use!
```

---

## Main Interface Buttons

```
┌─────────────────────────────┐
│  FACE RECOGNITION SYSTEM    │
├─────────────────────────────┤
│ Camera Feed    │  Controls  │
│                │            │
│                │ Register   │
│                │ Attendance │
│                │ Dashboard  │
│                │ Admin      │
│                │ Student    │
└─────────────────────────────┘
```

---

## 3-Step Usage

### Step 1: Register a Student
- Enter name
- Click "Start Registration"
- Face camera for 30 samples
- Done! (Automatic)

### Step 2: Mark Attendance Daily
- Click "Mark Attendance"
- Face camera
- Click "Accept & Mark Attendance"
- Done!

### Step 3: Check Records
- Click "Student Dashboard"
- Select student name
- View attendance history
- Export to CSV if needed

---

## Files You'll See Created

After first run:
```
✓ attendance.db          (database with all records)
✓ images/                (face samples folder)
✓ face_recognizer.yml    (trained AI model)
✓ labels.pkl             (student mappings)
✓ Attendance_*.csv       (daily records)
```

All created automatically! No setup needed.

---

## Troubleshooting

| Problem | Solution |
|---------|----------|
| Can't find RUN.bat | Make sure you're in correct folder |
| Camera won't open | Close Zoom/Teams, check permissions |
| Face not detected | Better lighting, move closer |
| "No model found" | Register at least 1 student first |
| Python not found | Install Python from python.org |

---

## All Files in Project

```
main.py                    ← Run this OR use RUN.bat
database.py              ← Database manager (auto-used)
attendance_page.py       ← Attendance window (auto-opened)
admin_dashboard.py       ← Admin window (auto-opened)
student_dashboard.py     ← Student window (auto-opened)
RUN.bat                  ← Easy start script
RUN.ps1                  ← PowerShell start script
SETUP_GUIDE.md           ← Detailed manual (read for details)
attendance.db            ← Database (auto-created)
images/                  ← Face samples (auto-created)
face_recognizer.yml      ← AI model (auto-created)
labels.pkl               ← Labels mapping (auto-created)
```

---

## System Workflow

```
START
  ↓
Run RUN.bat or python main.py
  ↓
MAIN WINDOW OPENS
  ├─→ Register Students
  │    └─→ Captures 30 face samples
  │    └─→ Trains AI model
  │    └─→ Saves to database
  │
  ├─→ Mark Attendance
  │    └─→ Opens attendance page
  │    └─→ Detects face
  │    └─→ Saves to database
  │
  ├─→ Admin Dashboard
  │    └─→ Manage students
  │    └─→ Add/Delete records
  │
  └─→ Student Dashboard
       └─→ View records
       └─→ Export CSV
```

---

## Database Info

Everything is stored in `attendance.db`:
- Student names and info
- Attendance records with timestamps
- Persists between runs
- Backup regularly!

---

## Example: Your First Run

```
1. Double-click RUN.bat
2. Window opens
3. Enter "John Doe"
4. Click "Start Registration"
5. Face camera (green box shows detection)
6. Wait 30 seconds for samples
7. "Registration complete!"
8. Click "Start Attendance"
9. Face camera
10. Click "Accept & Mark Attendance"
11. "Attendance marked!"
12. Click "Student Dashboard"
13. Select "John Doe"
14. See attendance record!
```

All done in ~2 minutes!

---

## Key Points

✅ **No Database Setup** - Auto-created  
✅ **No Configuration** - Works out of box  
✅ **No Internet Needed** - Fully offline  
✅ **One Command** - Just run RUN.bat  
✅ **Auto Trains** - Model builds after registration  
✅ **One-Click Attendance** - Easy marking  
✅ **Detailed Records** - Full history stored  

---

## Still Having Issues?

1. Read `SETUP_GUIDE.md` for detailed help
2. Check Python installed: `python --version`
3. Check packages: `pip list`
4. Verify camera works in other apps
5. Restart computer if needed
6. Delete `attendance.db` to reset database

---

## Next Steps After First Run

1. **Register all students** in admin panel
2. **Daily attendance** using attendance page
3. **Weekly reviews** in student dashboard
4. **Monthly exports** for records
5. **Periodic backups** of database

---

**Ready? Double-click RUN.bat and start using the system!**

For detailed documentation: Open `SETUP_GUIDE.md`
